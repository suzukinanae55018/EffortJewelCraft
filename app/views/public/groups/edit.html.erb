<% if @group.errors.any? %>
  <% if @group.errors.count > 1 %>
    <p><%= @group.errors.count %>errors prohibited this diary_record from being saved:</p>
  <% else %>
    <p><%= @group.errors.count %>error prohibited this diary_record from being saved:</p>
  <% end %>
  <ul>
    <% @group.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto mt-3">
      <h3>グループ編集</h3>
      <%= render 'form', group: @group %>
      <%=link_to "グループ一覧へ",groups_path %>
    </div>
  </div>
</div>